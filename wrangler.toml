# Cloudflare Pages Configuration
name = "survivornet"
compatibility_date = "2024-03-15"

# Important: Cloudflare Pages (BETA) wrangler.toml does not support the [build] section.
# The build command and root directory should be set in the Cloudflare Dashboard.

# This path is relative to the repository root
pages_build_output_dir = "SurvivorNet-React/dist"
